# URGENT FIXES - Проблемы на скриншотах

## 🔴 Проблемы которые вижу:

1. **Cyberpunk 2077 (#1091500) в списке** - classification: rejected, но не должен быть вообще
2. **Steam #730 и Steam #570** - это CS2 и Dota, не должны быть в candidates
3. **Имена не обогащены** - "Steam #730" вместо нормального имени
4. **Таймаут 30s** - старый таймаут (должен быть 180s)

## ✅ Что исправлено:

1. ✅ **Фильтрация blacklist в candidates** - теперь мегахиты не показываются
2. ✅ **Endpoint cleanup_blacklist** - для деактивации существующих мегахитов
3. ✅ **SCANNER_BUILD_ID обновлён** - "2026-01-16_16-45"
4. ✅ **UI timeout** - уже 180s в коде

## 🚀 Команды для немедленного исправления:

### 1. Деактивировать существующие мегахиты
```bash
curl -X POST http://localhost:8000/api/v1/relaunch/admin/cleanup_blacklist | jq
```

### 2. Проверить что build_id обновился
```bash
curl http://localhost:8000/api/v1/relaunch/health | jq '.scanner_build_id'
```
**Должно быть:** `"2026-01-16_16-45"`

### 3. Проверить что мегахиты исчезли из candidates
```bash
curl http://localhost:8000/api/v1/relaunch/candidates | jq '.[] | select(.steam_app_id == "1091500" or .steam_app_id == "730" or .steam_app_id == "570")'
```
**Должно быть:** пустой вывод

### 4. Обновить страницу в браузере (Ctrl+F5 или Cmd+Shift+R)

---

## 📝 Что изменилось в коде:

1. **`/candidates` endpoint:**
   - Добавлена фильтрация по `EXCLUDE_APP_IDS` (blacklist по app_id)
   - Добавлена фильтрация по `EXCLUDE_NAME_CONTAINS` (blacklist по имени)
   - Фильтрация происходит ДО возврата результата

2. **Новый endpoint `/admin/cleanup_blacklist`:**
   - Деактивирует все игры из blacklist
   - Работает по app_id и по имени
   - Возвращает количество деактивированных

3. **SCANNER_BUILD_ID:** обновлён для проверки применения кода

---

## ⚠️ Важно:

После выполнения `cleanup_blacklist` нужно:
1. Обновить страницу в браузере (hard refresh)
2. Проверить что мегахиты исчезли
3. Если имена всё ещё "Steam #id" - запустить новый scan (он обогатит имена)
