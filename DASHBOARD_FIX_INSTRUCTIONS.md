# Инструкция: Обновление дашборда

## Проблема
Дашборд не обновляется после изменений (кэш браузера).

## Решение

### 1. Перезапустите API
```bash
docker compose restart api
# или
docker compose down && docker compose up -d
```

### 2. Очистите кэш браузера

**Chrome/Edge:**
- Нажмите `Ctrl+Shift+R` (Windows/Linux) или `Cmd+Shift+R` (Mac) для жесткой перезагрузки
- Или откройте DevTools (F12) → вкладка Network → включите "Disable cache"

**Firefox:**
- Нажмите `Ctrl+F5` (Windows/Linux) или `Cmd+Shift+R` (Mac)
- Или откройте DevTools (F12) → вкладка Network → включите "Disable cache"

### 3. Проверьте консоль браузера

Откройте консоль (F12) и проверьте логи:
- Должно быть: `🚀 Initializing dashboard...`
- Должно быть: `Found 6 tabs`
- Должно быть: `✅ Active tab found: system`
- Должно быть: `🔄 Switching to tab: system`

Если этих логов нет — значит старый файл всё ещё загружается.

### 4. Проверьте версию дашборда

В консоли браузера выполните:
```javascript
document.querySelector('meta[name="dashboard-version"]')?.content
```

Если версия не меняется после перезапуска API — проблема в кэше.

### 5. Альтернатива: откройте в режиме инкогнито

Откройте `http://localhost:8000/dashboard` в режиме инкогнито/приватном режиме — там кэш не используется.

## Что было исправлено

1. ✅ Добавлена функция `initializeDashboard()` с логированием
2. ✅ Улучшена функция `switchTab()` с отладкой
3. ✅ Добавлены заголовки `Cache-Control` для отключения кэша
4. ✅ Добавлено версионирование дашборда

## Проверка работоспособности

После перезапуска API и очистки кэша:
1. Вкладки должны быть видны и кликабельны
2. При клике на вкладку должна переключаться панель
3. В консоли должны быть логи инициализации
4. Данные должны загружаться при переключении вкладок
