# Emerging Engine v4 — Статус реализации

## ✅ Выполнено

### 1. Создан упрощённый Emerging Engine v4
- ✅ `apps/worker/analysis/emerging_engine_v4.py` — новый модуль
- ✅ Формула: `log1p(recent_reviews_count_30d) * all_positive_ratio`
- ✅ Фильтры: Evergreen, Quality, Growth, Score Threshold
- ✅ Строгий набор вердиктов (6 вариантов)

### 2. Обновлён diagnostics endpoint
- ✅ Использует только `steam_review_daily`
- ✅ Убраны JOIN с несуществующими таблицами
- ✅ Использует `analyze_emerging()` вместо `TrendsBrain`

### 3. Обновлён SQL запрос в emerging endpoint
- ✅ Убраны JOIN с `trends_raw_signals`
- ✅ Использует только `steam_review_daily` и `steam_app_cache`

## ⚠️ Требует доработки

### 1. Emerging endpoint — блок анализа
**Файл:** `apps/api/routers/trends_v1.py` (строки ~620-777)

**Проблема:** Всё ещё использует `TrendsBrain.analyze_game()` вместо `analyze_emerging()`

**Нужно:**
- Заменить весь блок анализа на использование `analyze_emerging()`
- Убрать обработку Reddit/YouTube/Steam News сигналов
- Упростить формат ответа

### 2. System/summary — упоминания несуществующих таблиц
**Файл:** `apps/api/routers/system_admin.py`

**Проблема:** Много упоминаний `trends_raw_signals` и `trend_jobs`

**Нужно:**
- Проверить, существуют ли эти таблицы в реальности
- Если нет — заменить на использование `steam_review_daily`
- Если да — оставить, но убрать из emerging логики

## 🎯 Следующие шаги

1. **Проверить существование таблиц:**
   ```sql
   SELECT table_name FROM information_schema.tables 
   WHERE table_schema = 'public' 
   AND table_name IN ('trends_raw_signals', 'trend_jobs', 'trends_game_daily');
   ```

2. **Завершить рефакторинг emerging endpoint:**
   - Заменить блок анализа (строки ~620-777)
   - Использовать `analyze_emerging()` вместо `TrendsBrain`

3. **Обновить system/summary:**
   - Использовать только реальные источники
   - Показывать честное состояние системы

## 📝 Принципы (из ТЗ v4)

✅ **Соблюдено:**
- Emerging считается on-the-fly
- Используется только Steam Reviews
- Формула упрощена
- Вердикты ограничены строгим набором

⚠️ **Требует проверки:**
- Все упоминания несуществующих таблиц удалены
- System/summary честно показывает состояние
- Emerging = 0 без ошибок (если сигналов нет)
